
<head>

</head>

<h1 class="unid_username"><%= @user.username.capitalize %></h1>

<ul>
  <% @user.profiles.each do |profile| %>
  <%= render partial: 'card', locals: {profile: profile}%>
  <% if profile.provider == 'twitch' %>
  <%= render partial: 'live', locals: {profile: profile} %>
  <% end %>
  <% end  %>
</ul>

<% if current_user && current_user == @user %>

<div class="unid_button"><%= link_to "Add A New Card", edit_user_path(@user.username), class: "btn btn-success" %></div>

<% end %>
