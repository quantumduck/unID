<h1><%= @user.username.capitalize %></h1>

<ul>
  <% @user.profiles.each do |profile| %>
  <%= render partial: 'card', locals: {profile: profile}%>
  <% end  %>
</ul>

<% if current_user && current_user == @user %>

<%= link_to "Add A New Card", edit_user_path(@user.username) %>

<% end %>
