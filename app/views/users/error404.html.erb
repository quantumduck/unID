<div class="cardset">
  <div class="container">
    <div class="row">
      <div class="newbiecard">
    <h1>404'd!</h1>
    <p>We don't know this <%= @user.username %> person. Please send them this link to sign up! </p>
       <p><%= link_to "#{root_url}#{@user.username}", "/#{@user.username}", class: "display-link" %><p>

    <%= render partial: "users/signup", locals: { user: @user } %>
    <img class ="unidcorn_new" <%= image_tag "unidcorn-icon2.png" %>
  </div>
    </div>
  </div>
</div>
