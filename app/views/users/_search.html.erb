<%= form_tag '/find', method: 'get' do %>
  <span class="field">
    <!-- <%= label_tag :profile_network_id %>? -->
    <%= select_tag :profile_network_id, options_for_select(provider_list), value: 8 %>
  </span>

  <span class="field">
    <!-- <%= label_tag :profile_name %> -->
    <%= text_field_tag :profile_name, nil, placeholder: "Find a user" %>
  </span>

  <span class="actions"><%= submit_tag "Search" %></span>
<% end  %>
